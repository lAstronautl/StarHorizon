# SS13 paths dropped on purpose. Prefixes, matched down the path tree.
#
# Everything here is a decision already made, not a guess -- delete a line and
# those paths come back in the scan report for you to decide on individually.

# Lives in the same source file as mapping_helpers above (code/modules/
# mapping/mapping_helpers.dm) and does the same job for baseturfs
# specifically -- "Set the baseturfs of every turf in the /area/ it is
# placed", blank icon_state, nothing to render.
- /obj/effect/baseturf_helper

# /obj/effect/gibspawner: icon_state exists "for the map editor" only
# (source comment) -- at runtime it spawns randomized blood-gib decals and
# self-deletes (return INITIALIZE_HINT_QDEL), the same spawn-then-vanish
# pattern as mapping_helpers above. The gibs it produces are randomized
# scatter, not something a static conversion could place correctly anyway.
- /obj/effect/gibspawner

# SS14 has no areas at all: rooms are defined by their walls and by the station
# map prototype, not by an area object covering the tiles. All 171 area paths of
# a typical /tg/ station would otherwise land in the report with nothing useful
# to map them to.
- /area

# Atmospheric pipes and power cables. SS13 and SS14 build these networks
# differently enough that a converted layout is worth less than the time spent
# untangling it -- the pipes carry layers, colours and directions that do not
# survive, and the cable net has to be re-run against SS14's power model anyway.
# Dropping them leaves clean floors to lay new ones on. On a /tg/ MetaStation
# this is about a third of all objects: 5857 pipes, 3631 cables.
#
# The machines that sit on these networks (vents, scrubbers, pumps, injectors)
# are still converted -- they are placed correctly and only need connecting up.
- /obj/machinery/atmospherics/pipe
- /obj/structure/cable
- /obj/machinery/power/terminal

# tgstation's plumbing subsystem: a second, separate pipe network (fluid
# ducts, code/modules/plumbing/) that moves chemistry/hydroponics reagents
# between machines, layered independently of the atmos pipe network above.
# This fork has no matching reagent-piping system at all (checked
# Resources/Prototypes for "plumbing"/"ductnet"/"fluid duct", nothing but an
# unrelated substring hit) -- same reasoning as the atmos pipes/cables above,
# not worth converting a network layout for a system that doesn't exist here.
# The item form (a wrenchable stack you place to build the network) is the
# same absent system.
- /obj/machinery/duct
- /obj/item/stack/ducts

# Map-init helpers. They tag or reconfigure the object next to them (set an
# airlock's access, break a floor tile, wire an atmos device) and then delete
# themselves -- confirmed in tgstation's own code/modules/mapping/mapping_helpers.dm,
# every one of these calls qdel(src) once its effect is applied and none of
# them is ever rendered. There is nothing here for a converted map to keep.
- /obj/effect/mapping_helpers

# SS13 features this fork's SS14 does not implement at all -- not renamed, not
# hidden elsewhere, genuinely absent from Resources/Prototypes. A newscaster is
# a physical bulletin board object; nav beacons and status displays are
# wayfinding/announcement hardware. None of the three exist here.
- /obj/machinery/newscaster
- /obj/machinery/navbeacon
- /obj/machinery/status_display

# Checked from several angles (exact id, keyword search, direct grep for the
# id pattern) and confirmed genuinely absent from this fork's SS14, the same
# way newscaster/navbeacon/status_display were above -- not renamed, not
# hidden, the feature or fixture just isn't implemented.
- /obj/machinery/light_switch
- /obj/machinery/requests_console
- /obj/machinery/digital_clock
- /obj/machinery/photocopier
- /obj/machinery/iv_drip
- /obj/machinery/shower
- /obj/structure/ladder
- /obj/structure/transit_tube
- /obj/structure/transport/linear
- /obj/structure/marker_beacon
- /obj/item/pai_card
- /obj/item/newspaper

# Every /obj/effect/landmark is invisible by construction --
# invisibility = INVISIBILITY_ABSTRACT is set on the base class itself
# (code/game/objects/effects/landmarks.dm), so there is nothing visual any of
# them are worth preserving. Job spawn points (landmark/start/<job>) are the
# one subtree with a real SS14 counterpart (SpawnPointJob markers) and are
# mapped in entities.yml instead -- everything below is tgstation-specific
# game logic (a minigame arena, event/loot-spawning systems, nav beacons, a
# unit-test-only sanity marker) with no matching system in this fork to wire
# up, so there is genuinely nothing to convert them to, the same reasoning as
# /area above.
- /obj/effect/landmark/awaystart
- /obj/effect/landmark/thunderdome
- /obj/effect/landmark/event_spawn
- /obj/effect/landmark/generic_maintenance_landmark
- /obj/effect/landmark/navigate_destination
- /obj/effect/landmark/firealarm_sanity
- /obj/effect/landmark/blobstart
- /obj/effect/landmark/carpspawn
- /obj/effect/landmark/secequipment

# Three more turfs in the same "genuinely not a real map tile" category as
# /area above, each confirmed by source:
#   /turf/template_noop      -- a sentinel the DMM reader checks for (reader.dm)
#                                to mean "skip placing a turf here", not a turf
#                                that is ever actually placed.
#   /turf/cordon              -- the invisible, indestructible world-border turf
#                                (invisibility = INVISIBILITY_ABSTRACT,
#                                code/modules/awaymissions/cordon.dm).
#   /turf/open/ai_visible     -- background tile of the AI multicam HUD's
#                                viewing room, off-map (plane -19, SPACE_LAYER,
#                                code/modules/mob/living/silicon/ai/multicam.dm),
#                                never a placeable station floor.
- /turf/template_noop
- /turf/cordon
- /turf/open/ai_visible

# Same "invisible map-logic marker, self-deletes, nothing to convert" pattern
# as /obj/effect/mapping_helpers above, confirmed by source on each:
#   /obj/modular_map_connector and /obj/modular_map_root -- both
#     invisibility = INVISIBILITY_ABSTRACT (modular_map_loader.dm); root loads
#     a random sub-map from a .toml config at Initialize() and never renders.
#   /obj/docking_port (and every subtype, incl. /stationary) --
#     invisibility = INVISIBILITY_ABSTRACT (shuttle.dm) -- a coordinate marker
#     for tgstation's shuttle subsystem, which SS14 does not share the data
#     model of, the same reasoning as the pipe/cable networks above.
#   /obj/structure/fans/tiny/invisible -- invisibility = INVISIBILITY_ABSTRACT
#     (fans.dm), an airflow-blocker logic marker, not a real fan.
- /obj/modular_map_connector
- /obj/modular_map_root
- /obj/docking_port
- /obj/structure/fans/tiny/invisible

# More logic-only landmark subtrees (see the comment above the first batch
# of these for the general reasoning) -- each is a marker for a
# tgstation-specific system this fork doesn't implement: the away-mission
# hotel elevator, the lavaland mining event, ERT event spawning, the
# automated atmos self-test framework, the AI multicam viewing room
# (paired with the ai_visible turf already ignored above), the CTF and
# Mafia minigames, and the procedural river system (river_waypoint's own
# source sets invisibility = INVISIBILITY_ABSTRACT explicitly, same as the
# base landmark class every one of these already inherits it from).
- /obj/effect/landmark/prisonwarp
- /obj/effect/landmark/ert_spawn
- /obj/effect/landmark/atmospheric_sanity
- /obj/effect/landmark/mining_center
- /obj/effect/landmark/ai_multicam_room
- /obj/effect/landmark/ctf
- /obj/effect/landmark/mafia_game_area
- /obj/effect/landmark/river_waypoint
- /obj/effect/landmark/transport
# Another batch of minigame/antagonist-specific spawn markers, same "no
# matching system in this fork" reasoning as the batch above -- each
# verified by source: deathmatch (deathmatch_mapping.dm), mafia
# (mafia/map_pieces.dm), the whole bitrunning/* subtree
# (bitrunning/objects/landmarks.dm), basketball (basketball_map_loading.dm),
# the shuttle arena minigame (area/areas/shuttles.dm), the voidwalker and
# abductor antagonist roles (their own antagonist modules), and the
# holding-facility/ert-shuttle/nukeop-elevator/nukeop-base event markers
# (game/objects/effects/landmarks.dm) -- none of these game modes or
# antagonist roles exist in this fork.
- /obj/effect/landmark/deathmatch_player_spawn
- /obj/effect/landmark/mafia
- /obj/effect/landmark/bitrunning
- /obj/effect/landmark/basketball
- /obj/effect/landmark/shuttle_arena_entrance
- /obj/effect/landmark/shuttle_arena_safe
- /obj/effect/landmark/voidwalker_void
- /obj/effect/landmark/abductor
- /obj/effect/landmark/holding_facility
- /obj/effect/landmark/ert_shuttle_spawn
- /obj/effect/landmark/start/nukeop_elevator
- /obj/effect/landmark/start/nukeop_base

# /obj/effect/step_trigger (teleporter, thrower, message, sound_effect, ...):
# invisibility = INVISIBILITY_ABSTRACT set on the base class itself
# ("nope cant see this shit" -- code/game/objects/effects/step_triggers.dm),
# the same invisible-logic-marker pattern as the landmarks above. The
# teleporter variant's target is also literal fixed x/y/z coordinates baked
# into the source map, which wouldn't mean anything in a converted map's
# coordinate space even if it were visible.
- /obj/effect/step_trigger

# The Xenomorph antagonist subsystem (hive weeds, resin walls/membranes,
# eggs, the drone/hunter/etc. castes themselves) has no equivalent in this
# fork at all -- checked from several angles (exact id, "xeno"/"alien"/
# "hivemind"/"facehugger" keyword search across Resources/Prototypes) and
# found nothing but unrelated substring hits (xenoarchaeology artifacts,
# flavour text). Not renamed or hidden elsewhere, the whole gameplay system
# just isn't implemented, the same reasoning as newscaster/navbeacon above.
- /obj/structure/alien
- /mob/living/basic/alien
- /obj/item/clothing/mask/facehugger

# /obj/effect/light_emitter (and /thunderdome, /fake_outdoors, /podbay):
# invisibility = INVISIBILITY_ABSTRACT, no sprite of its own -- it exists
# purely to call set_light() and fake ambient lighting near lava/glowing
# terrain in mining ruins (code/modules/mining/mine_items.dm). Every light
# source this fork's prototypes define is attached to a real, visible
# fixture (a wall light, a lamp post, a crystal, ...) -- there is no
# "invisible light with no fixture" concept to place it as.
- /obj/effect/light_emitter

# /obj/effect/particle_effect (and every subtype -- water/extinguisher incl.
# the acid-spit variant, smoke, foam, sparks, explosion, trail): the runtime
# VFX base class behind tgstation's effect_system, confirmed by source
# (code/game/objects/effects/effect_system/*) -- every instance is created
# with `new()` from mob/machine code (a spat acid glob, an extinguisher jet,
# a smoke machine puff) and moves/times out on its own. Not something a
# mapper places, the same reasoning as step_trigger above.
- /obj/effect/particle_effect

# Three more structures checked from several angles (exact id, keyword
# search) and confirmed genuinely absent from this fork's SS14, the same
# way newscaster/navbeacon/status_display were above -- not renamed, not
# hidden, the fixture just isn't implemented:
#   /obj/structure/hedge (and /opaque) -- a smoothing garden-maze structure
#     (modules/hydroponics/grown/hedges.dm), no Hedge* entity anywhere.
#   /obj/structure/foamedmetal (and /iron, /resin) -- the wall a chemistry
#     metal-foam reaction leaves behind (effects_foam.dm); this fork's own
#     metal foam is a grenade effect only, no placeable resulting wall.
#   /obj/structure/barricade/sandbags -- a smoothing sandbag cover
#     (game/machinery/deployable.dm), no Sandbag* entity anywhere.
- /obj/structure/hedge
- /obj/structure/foamedmetal
- /obj/structure/barricade/sandbags

# /turf/open/openspace (and every subtype -- icemoon, icemoon/ruins,
# icemoon/keep_below, airless, telecomms, coldroom, xenobio): confirmed by
# source (game/turfs/open/openspace.dm) to be a "hole in the floor" -- it
# isn't drawn at all ("We don't actually draw openspace"), passes atmosphere
# and light straight through, and drops anything standing on it to the z-level
# below (zFall() on Entered()). A tile prototype has no way to express any of
# that; this is a mechanic, not a sprite, the same reasoning as the atmos
# pipe network above.
- /turf/open/openspace

# /turf/open/water (and every subtype -- deep_beach, beach, jungle, lavaland_atmos,
# ...): tgstation's swimming/drowning terrain (water.dm). This fork has no
# water or swimming system at all -- checked from several angles (a Tiles
# prototype search for "water"/"beach" turns up nothing, and there is no
# Swimming/WaterComponent/DeepWater anywhere in Content.Server or
# Content.Shared) -- not renamed or reskinned, the feature just isn't
# implemented, same reasoning as newscaster/navbeacon above. The walkable
# (non-swimming) sand next to it, /turf/open/misc/beach/sand and
# /turf/open/misc/beach/coast, has no equivalent either and is left
# unresolved rather than force it onto an asteroid tile that isn't sand for
# the same reason.
- /turf/open/water

# /turf/open/genturf (plain, /blue, /orange): not a real tile, a debug
# placeholder. Its own source comment says so outright -- "This turf doesn't
# actually do anything beyond provide contrast for mappers and be very
# visible when stuff breaks in game. [...] If you see this, and you're not a
# ghost, yell at coders" (code/datums/mapgen/JungleGenerator.dm). The map
# generator's generate_terrain() overwrites every genturf tile with a
# procedurally chosen biome turf before a round starts -- it is never the
# tile a player actually sees, so there is no fixed sprite to convert it to.
- /turf/open/genturf

# ---- b0ee8406-sq3mub.dmm ruin: checked against tgstation source and the
# full Resources/Prototypes tree (forks included), no match found ----

# /obj/effect/mob_spawn/ghost_role/human: a generic ghost-role spawn point
# whose name/outfit/flavour_text/gender are all set per-instance directly in
# the map file (9 distinct instances here, each customized). No SS14 marker
# supports that level of ad-hoc per-instance customization.
- /obj/effect/mob_spawn/ghost_role/human
# /obj/item/poster/random_contraband: the *portable, rolled-up* poster item
# (game/objects/effects/posters/poster.dm) -- SS14's poster spawners
# (RandomPosterContraband et al.) are wall-mounted structure spawners only,
# there is no held/loose poster item form to match against.
- /obj/item/poster/random_contraband
- /obj/structure/urinal
# /obj/structure/disposaloutlet: SS14's disposal system has no outlet
# structure at the far end of the pipe network (checked Entities/Structures/
# Piping/Disposal/*.yml -- only units, pipes and fittings, no outlet).
- /obj/structure/disposaloutlet
# /obj/item/pipe_dispenser: the RPD (rapid pipe dispenser) hand tool
# (game/objects/items/rcd/RPD.dm) -- no RPD/RapidPipe id anywhere.
- /obj/item/pipe_dispenser
- /obj/item/surgical_drapes
- /obj/item/trash/can
- /obj/machinery/dna_scannernew
- /obj/machinery/mech_bay_recharge_port
# /obj/item/coin (bare): the concrete base coin type itself (not abstract),
# but SS14 has no generic "coin" -- only the specific treasure coins already
# mapped (TreasureCoinDiamond3/Silver3/Iron3) and fork-specific currencies.
- /obj/item/coin
- /obj/item/gun/ballistic/automatic/pistol/suppressed
- /obj/item/gun/medbeam
# /obj/item/megaphone: no Loudspeaker/Bullhorn/Megaphone id anywhere.
- /obj/item/megaphone
# /obj/item/razor: no shaving razor item; only unrelated cosmetic/hair
# matches (BarberScissors, HumanHairShaved, SignBarbershop, ...).
- /obj/item/razor
- /obj/item/seeds/harebell
# /obj/item/stock_parts/micro_laser, /scanning_module: SS14 only models
# three stock-part families (Capacitor/Manipulator/MatterBin), tgstation's
# extra micro-laser/scanning-module tiers have no equivalent.
- /obj/item/stock_parts/micro_laser
- /obj/item/stock_parts/scanning_module
# /obj/item/storage/belt (bare): abstract_type in tgstation source -- never
# placed directly, so its presence here is itself a bug in the source map.
- /obj/item/storage/belt
- /obj/item/surgery_tray/full
- /obj/item/trash/sosjerky
# /obj/item/wallframe/camera: tgstation's portable "camera assembly" item
# you carry and apply to a wall. SS14's nearest equivalent,
# SurveillanceCameraAssembly, is instead a permanently anchored, wall-snapped
# structure (Transform.anchored: true, placement snap: Wallmount) with no
# item form -- and this instance has no dir/pixel_x/pixel_y in the source
# map, i.e. it's loose floor loot, not something already on a wall.
- /obj/item/wallframe/camera
# /obj/machinery/airlock_controller/incinerator_atmos: part of tgstation's
# tag-linked embedded-controller airlock automation (wires up doors/pumps/
# sensors by id_tag) -- this fork has no equivalent tag-based system.
- /obj/machinery/airlock_controller/incinerator_atmos
- /obj/machinery/bookbinder
- /obj/machinery/chem_heater
# /obj/machinery/conveyor_switch: no ConveyorSwitch id -- conveyor belts are
# interacted with directly (ConveyorBelt), no separate switch entity.
- /obj/machinery/conveyor_switch
- /obj/machinery/gibber
- /obj/machinery/libraryscanner
# /obj/machinery/processor: dices/slices solid food items into sub-items
# (food_and_drinks/machinery/processor.dm). KitchenReagentGrinder was
# considered and rejected -- it juices items into liquid reagents, a
# different item-to-reagent mechanic, not item-to-item slicing.
- /obj/machinery/processor
- /obj/machinery/monkey_recycler
# /obj/machinery/pipedispenser (and /disposal, /disposal/transit_tube): the
# machine form of the pipe dispenser -- no PipeDispenser/PipeVend id anywhere.
- /obj/machinery/pipedispenser
- /obj/machinery/pipedispenser/disposal
- /obj/machinery/pipedispenser/disposal/transit_tube
# /obj/machinery/sleeper: the real automated-treatment sleeper machine.
# Substituting CryoPod was considered and rejected -- CryoPod is a
# stasis/preservation mechanic, not the sleeper's active reagent-based
# healing, so this is left unresolved rather than force a different mechanic.
- /obj/machinery/sleeper
# /obj/machinery/power/turbine (bare -- itself abstract_type in tgstation
# source, so its presence here is a bug in the source map -- and all real
# subtypes: /inlet_compressor, /turbine_outlet, /core_rotor): a standalone
# atmospheric turbine that turns a gas pressure differential from a pipe
# network into power (code/modules/power/turbine/turbine.dm). "Turbine" was
# mapped here for three of these paths and had to be undone -- SS14's only
# "Turbine" id (_FarHorizons/Entities/Structures/Power/Generation/
# FissionGenerator/turbine.yml) is a completely different apparatus: a
# gas turbine that is part of the fission/nuclear reactor generation
# complex (GuideHelp points at NuclearReactor, DeviceLinkSource/Sink drive
# a stator-load UI, PowerSupplier ramps up to 75000 W) -- a nuclear-reactor
# component, not a general-purpose atmos-to-power converter, so it is the
# wrong mechanic even though the name matches exactly.
- /obj/machinery/power/turbine
# /obj/machinery/vending (bare): abstract_type in tgstation source -- never
# placed directly, so its presence here is itself a bug in the source map.
- /obj/machinery/vending
- /obj/machinery/washing_machine
- /obj/structure/displaycase
- /obj/structure/reagent_dispensers/cooking_oil
# /obj/structure/safe, /obj/structure/safe/floor: a generic loot safe (and
# its floor-hidden variant). GunSafe was considered and rejected -- it's
# weapon-specific loot dressing, not a generic safe, so this is left
# unresolved rather than force a mismatched mechanic.
- /obj/structure/safe
- /obj/structure/safe/floor
# /obj/structure/sign/departments/evac: a plain, non-directional evacuation
# department sign (icon_state "evac"). Distinct from the arrow-shaped
# /obj/structure/sign/directions/evac (icon_state "direction_evac") that
# SignDirectionalEvac already covers -- no plain equivalent exists.
- /obj/structure/sign/departments/evac
# /obj/structure/transit_tube_pod: decorative transit-tube-system prop; this
# fork has no transit tube system at all (no TransitTube* id anywhere).
- /obj/structure/transit_tube_pod

# ---- closing every remaining tgstation /turf/ type against
# Resources/Prototypes/Tiles: checked all 900 turf type definitions in
# tgstation source (not just what one map happened to use). Anything not
# listed here or added to turfs.yml has a real SS14 target; everything below
# genuinely doesn't, for the reason given. ----

# /turf/open and /turf/closed themselves are abstract_type in tgstation
# source and BYOND can never instantiate them directly, so they need no
# ignore entry at all -- and must not get one, since ignore.yml matches by
# prefix (path == entry or path.startswith(entry + "/")), so "/turf/open"
# here would silently swallow every open turf in the game.
# /turf/baseturf_skipover (+ /shuttle, /trapdoor): tgstation's own source
# says "This shouldn't exist" and Initialize() stack_traces and deletes
# itself on the spot if one is ever instanced.
- /turf/baseturf_skipover
# /turf/open/floor/mineral (bare): icon_state is explicitly null in source --
# doesn't render at all unless a concrete subtype is used.
- /turf/open/floor/mineral
# /turf/open/floor/material (+ /meat, /meat/airless): a dynamic-material
# floor whose colour comes from whatever custom_materials built it
# (MATERIAL_COLOR flag) -- a runtime tinting mechanic, not a fixed sprite.
- /turf/open/floor/material
# /turf/open/misc (bare): tgstation's own placeholder for "coder/mapper
# fucked up", same as /turf/open/genturf above.
- /turf/open/misc
- /turf/open/floor/emissive_test
# /turf/open/floor/bluespace (bare, not the mineral one below): "micro-
# teleports [that] let people move at incredible speeds" -- a speed-boost
# mechanic, not a floor material.
- /turf/open/floor/bluespace
# /turf/open/floor/sepia: "Time seems to flow very slowly around these
# tiles" -- a time-dilation mechanic.
- /turf/open/floor/sepia
# /turf/open/floor/fakeice: holodeck illusion, deliberately NOT slippery
# despite looking like ice -- the opposite of what FloorIce would imply.
- /turf/open/floor/fakeice
# /turf/open/floor/fakepit: holodeck illusion, deliberately walkable despite
# looking like a bottomless pit.
- /turf/open/floor/fakepit
# /turf/open/floor/light (+ /colour_cycle, /colour_cycle/dancefloor_a/_b)
# and /turf/open/indestructible/light: an interactive light-emitting floor,
# colour set with a multitool, some variants auto-cycling -- a mechanic, not
# a sprite.
- /turf/open/floor/light
- /turf/open/indestructible/light
# /turf/open/misc/beach/coast (+ /corner): tied to the tide mechanic, same
# reasoning as /turf/open/water above.
- /turf/open/misc/beach/coast
# /turf/open/floor/black: paired in source with greenscreen (a camera/film
# gimmick set), no equivalent.
- /turf/open/floor/black
- /turf/open/floor/greenscreen
# /turf/open/floor/hauntium (+ /ghostbricked): Halloween event floor.
- /turf/open/floor/hauntium
# /turf/open/floor/neo (+ cyan/orange/purple/red): a joke tile family
# ("Radiates with the power of being pedantic about colour names"), no
# equivalent.
- /turf/open/floor/neo
# /turf/open/floor/sandstone (+ /basalt, /cobbled, /tiled): each variant is
# its own unique brick/vault pattern (checked icon_state per variant, none
# match anything in the SS14 tile catalog, including the coincidentally
# similarly-named "vault" tile which turned out to be a different sprite).
- /turf/open/floor/sandstone
# /turf/open/floor/silvergold (+ /flow, /sword, /void): unique decorative
# wizard-vault patterns (a sword, a starfield, a flowing pattern), no
# equivalent.
- /turf/open/floor/silvergold
# /turf/open/floor/bone (+ /corner, /meaty and all its children): lavaland
# dungeon dressing, no equivalent.
- /turf/open/floor/bone
# /turf/open/floor/meat: "Floor is meat!" -- dragon-lair/heretic event floor.
- /turf/open/floor/meat
- /turf/open/mirage
# /turf/open/misc/ashplanet (+ /ash, /rocky, /wateryrock and its children):
# no Ash-family tile anywhere in the catalog.
- /turf/open/misc/ashplanet
# /turf/open/misc/hay (+ /icemoon, /lavaland): no Hay tile anywhere.
- /turf/open/misc/hay
# /turf/open/cliff (+ /snowrock and its children): elevation-change cliff
# dressing, not a floor material.
- /turf/open/cliff
# The rest of /turf/open/indestructible/*: boss arenas, minigames and
# events, each checked individually and confirmed gimmick-only.
- /turf/open/indestructible/airblock
- /turf/open/indestructible/binary
- /turf/open/indestructible/bitrunning_transport
- /turf/open/indestructible/boss/air
- /turf/open/indestructible/hierophant
- /turf/open/indestructible/honk
- /turf/open/indestructible/hoteltile
- /turf/open/indestructible/hotelwood
- /turf/open/indestructible/meat
- /turf/open/indestructible/necropolis
- /turf/open/indestructible/paper
- /turf/open/indestructible/rockyground
# /turf/open/indestructible/tram and /turf/open/floor/tram (+ /plate and its
# children): the tram transport system doesn't exist in SS14 at all.
- /turf/open/indestructible/tram
- /turf/open/floor/tram
# lavaland ore-vein floors with no matching SS14 tile (gold/uranium/plasma
# are mapped above; these have nothing).
- /turf/open/floor/mineral/abductor
- /turf/open/floor/mineral/adamantine
- /turf/open/floor/mineral/bananium
- /turf/open/floor/mineral/bluespace
- /turf/open/floor/mineral/diamond
- /turf/open/floor/mineral/metal_hydrogen
- /turf/open/floor/mineral/mythril
- /turf/open/floor/mineral/plasteel
- /turf/open/floor/mineral/runite
- /turf/open/floor/mineral/telecrystal
# titanium/yellow (plain and /tiled): no SS14 shuttle-floor tile is yellow.
- /turf/open/floor/mineral/titanium/yellow
- /turf/open/floor/mineral/titanium/tiled/yellow
# /turf/open/floor/cult (+ /airless): cultist ritual-room engraved floor, no
# equivalent.
- /turf/open/floor/cult
# concrete/pavement ("hot, coarse pavement") and /reinforced ("hexacrete",
# a hexagonal pattern): distinct textures from the tile/slab/smooth family
# mapped above, no matching SS14 variant.
- /turf/open/floor/concrete/pavement
- /turf/open/floor/concrete/reinforced
# holodeck states that are themselves a mechanic, not just a sprite:
# beach/water ("gives the impression you can walk on water"), chess pattern,
# hyperspace ("may induce motion sickness"), space ("deadly aspects of space
# are not emulated here"), and stairs (a 3D structure, not a floor tile).
- /turf/open/floor/holofloor/beach/water
- /turf/open/floor/holofloor/chess_black
- /turf/open/floor/holofloor/chess_white
- /turf/open/floor/holofloor/hyperspace
- /turf/open/floor/holofloor/space
- /turf/open/floor/holofloor/stairs
